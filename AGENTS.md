# Prototype Instructions

- Desktop login and all three registration steps use a viewport-height split layout: the shared dark brand panel stays exactly `100dvh` tall while the form pane scrolls independently. On low-height desktop viewports the brand panel itself remains scrollable so its copy and footer are reachable. At `1100px` and below, hide the brand panel and restore normal single-column page scrolling without horizontal overflow.
- Forgot-password uses a generic email-enumeration-safe response, a 60-second resend wait, and a one-time 30-minute reset link. The local prototype exposes a clearly labeled demo link only when an eligible mock account exists. Completing reset applies the registration password rules, rejects the current password, consumes every outstanding link for the account, revokes all sessions, and records security notification/audit events. Administrator reset sends the same link and never changes or reveals a password directly.

Run the local server yourself and open the preview in the browser available to this environment. Do not give the user server-start instructions when you can run it.

Before making substantial visual changes, use the Product Design plugin's `get-context` skill when the visual source is unclear or no longer matches the current goal. When the user gives durable prototype-specific design feedback, preferences, or decisions, record them in `AGENTS.md`.

When implementing from a selected generated mock, treat that image as the source of truth for layout, component anatomy, density, spacing, color, typography, visible content, and hierarchy.

Build app UI in `src/`. Keep `.openai/hosting.json`, `worker/index.js`, `scripts/prepare-sites-build.mjs`, and `tests/sites-worker.test.mjs` intact so the same local prototype can be handed to Sites. Before a Sites handoff, run `npm run build` and `npm run test:sites`; the build must leave `dist/client/index.html`, `dist/server/index.js`, and `dist/.openai/hosting.json`.

## Prototype Decisions

- Login keeps passwords masked by default and provides the existing localized eye toggle inside the password field. Revealing or hiding the value must not change the password, submit the form, or alter login behavior.
- Administrator User Management → User Detail → Invoice shows only Invoice number, amount, status, and actions on desktop; its mobile cards likewise omit project and brand. Other Invoice lists and retained legacy administrator pages keep their existing fields.
- Administrator audit logs remain searchable and filterable in the interface but do not provide an export action or client-side CSV generation.
- Creator Invoice list shows four non-interactive contract-style summary cards for all active, creator-action, processing, and paid Invoices. `CANCELLED` records are excluded. Every other Invoice belongs to exactly one group so all equals the other three combined; payment failures stay creator-action until payout corrections are submitted, then move to processing. The search, review-status, and payment-status controls stay left-aligned and do not change summary totals.

- Creator profile social accounts have an independent “Add social account” action in view and profile-edit modes; administrator reuse stays read-only. A new HTTP(S) URL requires 1–6 PNG/JPEG account-dashboard screenshots (maximum 8 MB each). Screenshot bytes remain in browser IndexedDB while the profile stores file references and a per-URL local-demo verification result. This immediate mock result is not real ownership verification, does not change existing URL order, the first handle, overall verification/signature permission, open corrections, payout details, or unsaved profile edits. A failed file write does not add an account.
- The add-social-account dialog uses the registration step's single profile-link input styling, dashed screenshot upload zone, and removable file rows inside a rounded, internally scrollable modal. It still adds only one profile at a time; do not copy registration's multi-link or demo-fill actions into this dialog.

- The portal supports English (default for first visits or invalid saved values) and Chinese through i18next/react-i18next. A valid saved `lang` choice is preserved. The globe/current-language/chevron dropdown persists `lang` locally and updates the document language and title across authentication, creator, and administrator surfaces. Translation is a presentation concern: stable status codes, routes, validation behavior, stored notifications, audit history, and other business data must not be rewritten when language changes. Existing English-only brand-panel copy stays English in both languages.

- Creator registration reuses the dark login brand panel in place of the former photo story. The shared login/onboarding brand headline, description, and three benefit captions are English-only; form fields retain their existing Chinese/English wording.
- Public email and mock Google registration both require acceptance of the agreements and the local prototype invitation code `COMETS-DEMO-2026`, validated in the form and mock registration service. This code is for demonstration only, not a production invitation mechanism.
- Every registration/onboarding step offers an explicit `填入演示资料` action that fills only blank fields. It never auto-accepts agreements, verifies social ownership, submits a form, or creates a payout account. Social evidence uses a clearly labeled bundled demo SVG with a stable preview URL. Bank examples follow the currently loaded Airwallex Form Schema and preserve any manual input.
- Onboarding step three labels the legal name `真实姓名/公司名 / Real Name/Company Name` and reuses the profile's Bank Transfer/PayPal grouping with Airwallex/PayerMax provider selection. Only Airwallex is open; dynamic Schema validation and Beneficiary creation remain unchanged. Apply these updates only to the local test environment until an explicit push request.

- Administrator user activity records successful creator business actions (contract/Invoice signing, external Invoice upload, information correction, account selection, per-page confirmation and review submission, profile/payout changes, and payment-retry requests) alongside existing account audits. Do not log passive navigation, search, or filter changes; never include bank values or document contents in action summaries. Sensitive-value reveal distinguishes unfilled, masked, and revealed states, requires an audited reason, and clears revealed values on user/tab change. Historical actions that were never recorded cannot be reconstructed.

- The administrator creator-selection page has only `全部达人` and `待修正` quick filters; do not offer a `待认证` selection shortcut. The current `待修正` filter means any open field-level correction (including, but not limited to, payout fields). Incomplete or legacy pending-verification accounts remain visible under `全部达人` and manageable in user management; removing the shortcut does not by itself change account creation or verification timing.
- Administrator mode is an additive layer on creator routes. Admin login lands on `/admin/select-user`; a selected Creator ID is required on `/home`, `/contracts`, `/invoices`, `/payments`, and `/profile`, and the same creator page components remain in use. Creator `/` and interactions stay unchanged. Keep all legacy `/admin` business pages, source and routes for comparison and rollback; retain functional user management and audit logs. Do not delete legacy code or deploy to `192.168.88.188:8772` without explicit later approval.
- Administrator views are read-only for creator contracts, Invoice, payout, and payment operations. Recover the actor from the active session, validate the selected Creator ID in user management, derive data from the one persistent mock store, and mask payout/history fields. Unverified original PDF assets are never linked, previewed, or downloaded within admin UI. The external management link is `http://192.168.88.188:8771/` and passes no local session. Browser-only mock checks do not replace production backend authorization.

- Payment failure occurs after a transfer attempt was executed and rejected by the channel; it does not reopen external Invoice ticket review. The creator payout tab shows the channel failure reason, while ticket-vs-account differences remain informational. Its fixed lower action area has `修改收款信息 / 切换收款账户 / 确认重新打款`; profile save only saves, account switching stages an eligible alternate in a dialog, and final confirmation submits a finance-review request. An unchanged original account requires a second confirmation warning. The creator cannot initiate a payment attempt. Record the chosen mode and effective account snapshot on the same approved Invoice; do not discard failed attempts. Payout field results align to the row's right: green check for match, English `Mismatch` or `From linked account` otherwise.

- For external Invoice review, correcting simulated ticket values is optional when the current values already match the required issuer, Bill To, amount, and currency. Corrections preserve the original recognition and invalidate the Invoice-information confirmation. Both pages require confirmation before review submission; ticket payment fields never block or influence payout-account choice.

- External Invoice is a compliance attachment; payment uses the creator-selected verified account. Upload collects only the document; after recognition the `收款信息确认` tab requires an explicit account selection with no default for a new file, then shows its complete payout fields in compact read-only rows. Never compare the ticket payment fields with that account or require ticket-field corrections for payout confirmation. Preserve old recognition and historical confirmation snapshots; changing an account or its profile details invalidates only the current payout-page confirmation. Show `付款方式 / Payment method` as `Bank Transfer` or `PayPal`. Both confirmed pages suffice for review submission without another checkbox.

- External Invoice information uses compact read-only comparison rows, with date picker and currency select when editing. Its payout page instead shows only the selected profile account's complete fields in the same compact row anatomy, never the ticket payment fields or match badges. The administrator view remains read-only and masked.

- Pending-signature contracts do not link to Invoices or collection projects. Creator contract detail uses three independent status/amount/validity cards, a pre-signing reminder, and a 6:4 document/information desktop layout. Contract signature reuses the Invoice handwritten/generated-signature dialog, saves a visible local signature record, and never rewrites the shared demonstration PDF.

- This product now includes both the creator-facing COMETS payment portal and a system-administrator workspace focused on user and identity operations.
- Administrators and creators use the same login form without a role selector. The authenticated account's server-owned `ADMIN` or `CREATOR` role determines the destination workspace, and public registration can only create `CREATOR` accounts.
- Treat the administrator workspace as a user and identity operations console, not a payment management console. Administrators manage accounts, invitations, access status, verification, corrections, notifications, audit history, and user-management settings; they do not create contracts or Invoices, approve requests, or execute payments.
- Administrator user management does not expose account creation/invitation controls or invitation-status filters, columns, detail fields, dashboard metrics, or settings. Preserve historical invitation data, registration invitation-code validation, notifications, and audit events internally. Single and bulk enable/disable actions use a compact confirmation dialog without a reason field; status audits still record actor, subject, before/after state, and time.
- Contracts carry the stable type `INDEPENDENT | FRAMEWORK | IO`; legacy and seeded contracts default to `INDEPENDENT`, and invalid external values are rejected without overwriting stored data. Show the bilingual contract-type label only in Administrator User Management → User Detail → Contracts, where project and brand are intentionally hidden.
- Administrator routes use `/admin`, `/admin/users`, `/admin/users/:id`, `/admin/audit-logs`, and `/admin/settings`. The default administrator landing page is a compact user-operations overview.
- `ADMIN` can inspect creator business data in a clearly labeled selected-user context and may edit basic and social profile data. Administrators cannot sign an Invoice or edit a creator payout account; payout and other sensitive corrections must be returned at field level to the creator.
- Sensitive payout values are masked by default. Revealing a full value requires a reason and creates an audit event. User-list exports remain masked.
- Contracts, Invoices, request projects, and payment status are owned by an external system and enter through a typed, idempotent adapter keyed by immutable Creator ID. Administrator surfaces are read-only for these records.
- Keep creator account access status limited to `ACTIVE` and `DISABLED`; invitation, onboarding, verification, and correction state are tracked separately. Disabling retains data and revokes active access.
- Creator registration activates the account but restricts Invoice signing until social verification is complete. Initial verification is confirmed by an administrator; returned corrections auto-resolve after the creator submits values that pass the applicable validation.
- Apply administrator changes to the local test environment first. Never update `192.168.88.188:8772` unless the user explicitly asks to push.
- The authenticated home route is `/`; `/requests` redirects to `/`. Creators can view but cannot create or approve payment requests.
- In the creator workspace, the `/` sidebar label is `首页` and the matched contract-and-Invoice module is named `收款`. Creator-facing request identifiers, status, progress, search, empty states, return links, and help copy use `收款`; internal `RequestProject` names and `/requests` routes remain unchanged. The administrator workspace continues to use the business name `请款项目`, and contractual terms such as `请款内容` remain unchanged.
- Request projects group Invoices only by a backend-supplied immutable contract ID under the current creator and only while that contract is `ACTIVE`. Project names never infer a missing relationship. Pending/expired/foreign/unknown links are cleared from the creator view without removing Invoice history; they await a new valid backend reference. A contract may have many Invoices, and an Invoice may have many payment attempts but at most one successful payment.
- The creator home order is greeting, the original three amount cards, then unified `待办` and `最近更新` side by side. Keep the greeting and amount-card visual anatomy intact. At widths of 768px and above, the panels align at the top in a roughly 60/40 layout; below 768px, stack them without page-level horizontal overflow. Both panels use compact field-embedded cards and equal outer heights. When there are more than three to-dos, show exactly three complete cards at a time in an internally scrollable, keyboard-accessible region; keep all tasks available. Recent updates remain a non-scrolling three-record preview. Derive the common visible list height from the natural heights of the first three cards at the active width so wrapping never clips the third card.
- The creator-home greeting card uses the same subtle pink-to-blue gradient, fine border, and soft shadow as the creator profile summary banner; omit the former coral decorative circles while retaining its existing content and sizing.
- The three creator-home amount cards are mutually exclusive Invoice groups. `待处理金额` includes unsigned Invoices and payment failures still awaiting creator payout correction. `付款处理中金额` includes Invoices under review, approved and awaiting payment, actively processing payment, or in payment-failure recovery after the creator submitted payout corrections. `已打款金额` includes successful payments only. Uploaded-but-not-submitted, returned, and canceled Invoices do not count. Use the latest payment attempt before legacy payment fields, sum separately by Invoice currency, and keep each tooltip aligned with its definition.
- Home tasks rank actionable payment failures first, unsigned Invoices second, unsigned contracts third, then other actionable upload/confirmation/correction tasks. Recent updates are the three most recently changed Invoices, each with its linked contract and attempt-derived payment status. Empty states read `暂无待办` and `暂无更新`.
- Creator and administrator content canvases use the shared gray `--canvas` background. Home amount-card dots remain immediately before their labels in coral, blue, and green; new list cards use restrained blue/green accents and semantic red/amber/blue/green text badges.
- The UI mirrors the management console's light operational design and remains responsive down to 320px.
- Backend integrations are represented by typed service interfaces and a mock adapter.
- After an external Invoice payment fails, its payout tab retains the failed attempt's immutable account snapshot and timestamp. A separate in-page selector previews the current verified account proposed for retry; failure alone never disables the account. Editing opens that selected account in the profile, while an unchanged-account retry requires explicit confirmation and only submits a finance-review request, never a C-side payment.
- Invoice detail pages do not render a separate payment-attempt history card. Keep attempt data in the typed service for the payment timeline, failed-attempt payout snapshot, retry workflow, and `/payments` summaries; creator and administrator detail views rely on those focused surfaces instead of duplicating the attempt list.
- Creator external Invoice details explicitly label the editable ticket-field action `修改 Invoice 信息` while waiting for confirmation or after a field-correction return; a returned correction action opens the editor. Once review is submitted or approved, ticket information stays read-only, and administrator business views never expose creator edit actions.
- Léa Martin has one-time external Invoice demo records that merge into existing local data without resetting earlier progress: `INV-20260920-00002` and `INV-20260921-00001` start at `WAITING_UPLOAD` with no file or payout account; `INV-20260920-00003` remains `APPROVED` with a failed transfer, immutable attempt payout snapshot, and creator action pending. Never auto-reset them after interaction.
- Creator registration follows the supplied two-column reference: email, password, optional invitation code, required legal agreement, and Google registration. Personal name is collected during onboarding.
- Creator registration passwords must be 8–20 characters and include uppercase letters, lowercase letters, and numbers without spaces. Special characters are allowed but not required. The registration field states the complete rule and submission is blocked until every requirement passes.
- Creator registration must always leave its loading state after an unsuccessful email or Google attempt. Service errors such as a duplicate email or disabled public registration are shown in the existing form alert so the user can correct the input and retry without refreshing the page.
- Registration actions use the requested light-purple treatment. Social verification supports multiple profile URLs and up to six account-console screenshots, with both collections persisted in the profile model.
- On desktop registration, the creator image stays on the left and the form stays on the right. Registration actions use the COMETS mark's pink and light-purple palette.
- Social onboarding does not ask for a platform or account handle. It requires profile links, backend screenshots, and a successful ownership-verification state before continuing.
- Registration basic information uses the heading `基本信息` without a suffix and the fields `真实姓名 / Real Name`, `联系电话 / Tel`, `联系邮箱 / Email`, and `联系地址 / Address`; the profile mirrors the same field labels.
- The payout section is named “收款账户”. Airwallex is enabled; PayPal and PayerMax remain visible but disabled as “暂未开放”.
- The creator profile supports one payout account per payment channel, with channels ordered `Airwallex → PayPal → PayerMax`. Legacy same-channel duplicates are migrated by keeping the current default, otherwise the first verified usable account, otherwise the first record. Removed duplicates remain available only through immutable historical Invoice payout snapshots. Each retained account is a persistent card with its own provider, currency, masked identifier, verification state, and a top-right `MoreHorizontal` menu. The default account keeps a star beside that menu; `设为默认` exists only inside non-default account menus, with no separate section-level default action.
- The creator-facing payout-account section never displays Beneficiary status or Beneficiary generation counts. It uses three persistent channel selectors in the order `Airwallex → PayPal → PayerMax`; selecting a channel shows every account under that channel, while PayPal and PayerMax remain visibly marked as not yet available.
- `编辑档案` and payout-card `编辑账户` are separate edit modes. Account editing keeps social accounts and Invoice contact fields read-only, closes and hides payout-card operation menus, removes the top-right profile save controls, and provides `取消 / 保存修改` only at the bottom-right of the payout-information form.
- The payout-account list does not display the former `当前渠道 / 账户总数 / 可用账户 / 档案完整度` overview row. Every payout account instead has an internal `账户别名` that is separate from the beneficiary/account-holder name. New Airwallex accounts start unnamed and cannot be saved until a 2–40 character alias is provided; existing aliases are editable only through the payout-card `编辑账户` flow, normalize repeated whitespace on save, and update the account-card title after successful persistence.
- During payout-account creation or editing, the alias appears in a standalone `账户配置` card before the payout-scenario card. Its `Internal nickname` field is explicitly identified as an internal MUSE Pay field that is never submitted to the Airwallex Beneficiary API.
- Payout-account deletion is allowed only for draft, incomplete, validation-failed, or pending-confirmation accounts that have no Beneficiary, business linkage, transaction history, or active payment. Verified, Beneficiary-backed, or historically linked accounts are retained and can only be disabled; review, validation, and payment-processing states lock both destructive actions with an explanatory reason.
- A default payout account must be atomically replaced before deletion or disabling. Disabled accounts remain visible, are excluded from new-payment and default-account choices, and can be re-enabled without becoming default. Account mutations must refresh the default account, usable-account count, profile completeness, Beneficiary state, missing-information notice, and no-usable-account empty state.
- Airwallex fields are generated from country, currency, beneficiary type, and transfer method. Schema validation must pass before creating a Beneficiary and persisting its `beneficiary_id`.
- Every Airwallex payout scenario always starts with five required fields in this order: `Account name`, `Account number`, `Bank name`, `Bank address`, and `SWIFT code`. The Form Schema may append other scenario-specific fields, but it cannot remove or make these five optional; synonymous bank-address fields are normalized to one field.
- The saved profile must render the routing fields for the selected transfer scenario instead of showing an irrelevant empty SWIFT field.
- The creator profile does not expose the Airwallex Beneficiary ID. Its three verification-status badges are hidden while editing and restored after a successful save. Saving profile edits uses a blocking validation state followed by an explicit success dialog.
- The profile's certification screenshot action is labeled “上传认证截图” and uses a light-green treatment with the upload icon on the left.
- The sidebar account summary prefixes the creator handle with its current social platform, formatted as `平台 · @ID`.
- Each social-account card derives and displays that platform's own account name from its corresponding profile URL instead of reusing one shared handle.
- Editable profile field labels use the same Chinese / English wording as registration and onboarding. Section titles, descriptions, status badges, and action buttons remain Chinese-only.
- The required profile contact email uses the same email-format rule as registration, shows an inline error while editing, and blocks profile saving when the address format is invalid.
- The Airwallex payout-condition country field is labeled `国家 / Country` in registration and the saved profile.
- Airwallex account names accept only ASCII English letters and single spaces between name parts. Registration input sanitizes invalid characters live, while Schema validation and profile saving enforce the same rule.
- Basic and payout fields collected during registration remain required in the creator profile. The Airwallex full-field reference is merged by meaning so synonymous fields are not duplicated; fields absent from the active registration Schema appear dynamically in the profile as optional supplements, including personal ID fields or a company registration number according to beneficiary type.
- The profile supplemental-information area includes a compact reminder encouraging creators to complete as much information as possible so future payout-scenario changes require fewer document follow-ups, payment returns, and repeated edits.
- The creator profile does not collect or display `申报交易金额 / Mandatory trade amount` in supplemental payout information.
- Form labels mark required fields with `*`. Optional fields do not display `选填`, `Optional`, or another optional-state suffix.
- The creator profile follows the management-profile information hierarchy: creator summary, compact overview, social accounts, Invoice contact details, payout channel, a standalone payout-scenario section, and generated payout details.
- Profile-card header icon containers keep the existing 32 px anatomy but use distinct light system tones by section: blush for social accounts, blue for Invoice contact details, green for payout channel, lilac for payout scenario, and amber for Airwallex bank details.
- Country, recipient type, account currency, and transfer method are editable profile fields. Changing any one reloads the Airwallex Form Schema, removes fields that are no longer part of the selected scenario, and makes only the returned required fields mandatory before saving and recreating the Beneficiary.
- Generated Airwallex payout fields use the validation rules returned for the active country, currency, recipient type, and transfer method. Account name, bank account number, bank name, and IBAN show field-level errors on blur and save; IBAN validation covers country code, scenario length, and MOD-97 checksum before the Beneficiary validation call.
- Every generated Airwallex text input places its Schema-derived format requirement inside the input placeholder, covering allowed characters, exact or minimum/maximum length, and separator restrictions. Format hints are not repeated below inputs and do not include examples; registration and profile editing reuse the same rules.
- Generated Airwallex placeholders use a fixed 8.5px size without affecting entered values. Long IBAN requirements use a compact `国家开头｜总位数｜字符限制｜分隔符限制` format so the complete hint fits within the input.
- The active creator is Léa Martin. Contracts exist before Invoices. The current account has eight contracts: two `PENDING_SIGNATURE`, three `ACTIVE`, and three `EXPIRED`. Five contracts have six Invoices and five request projects: the existing `七月联名` contract has two Invoices; the other three contracts remain visible only in the contract module. A failed Invoice retains multiple historical payment attempts. No unrelated demo project is added. The C-side contract page uses four summary metrics, search, status filters, and a table/mobile list without an upload action. Every seeded contract opens a detail page and embeds the supplied 16-page `26-kol-standard-terms-template.pdf` with open-in-new-window and download access.
- Contract lifecycle is explicitly synchronized by the external contract system and is independent of Invoice review, payment, and failure states. The stable lifecycle is `PENDING_SIGNATURE → ACTIVE → EXPIRED`, displayed as `待签署 → 执行中 → 已过期`. Invoice transitions must never rewrite contract status. Contract detail places the full-width status board above a two-column workspace where the contract PDF viewer is on the left and the creator-obligation panel is on the right. The panels stack on narrow screens. Duties are extracted from the contract and each item is labeled by clause source and timing.
- Contract detail omits the former contract-source notice. Pending-signature detail retains a concise verification reminder: `请确认合同信息、收款信息准确无误；如有疑问请联系项目负责人，请勿签署。` The separate local-demo signing disclaimer remains visible.
- Contract-list amounts use the same font family, 10.5px size, and visual weight as the primary contract title instead of oversized amount typography.
- Contract-list actions reuse the Invoice-list interaction pattern on desktop and mobile: separate icon-only `查看` and `下载` controls with the same sizing, tooltip labels, and light-purple hover feedback. Mobile contract cards are not one large link; their title and action controls provide explicit navigation and download targets.
- Creator contract lists retain four summary cards and use the Invoice list's search/filter toolbar, bordered desktop rows, and responsive cards. Contract-file icons alone use light-blue backgrounds and blue strokes. Desktop columns share a consistent gap but content-aware widths; every field remains visible by wrapping long values and switching to labeled cards before columns become cramped. View and download remain independent actions.
- The creator contract list's search and status controls align to the left edge of the list toolbar, retain that order in two columns on wider screens, and stack in one column on narrow phones without page-level horizontal scrolling.
- Contract table headings align to the actual content starts: the first heading begins after the file icon and its gap, and `操作` begins at the left edge of the view/download control group. The other headings share their row columns unchanged; card layouts hide the header.
- All creator and administrator single-choice dropdowns share the compact COMETS treatment on desktop and mobile: a rounded trigger, white floating menu, pale-coral selected row with a check, visible keyboard focus, and a scrollable viewport-aware menu. Keep each dropdown's existing options and business behavior; button menus and date controls are separate patterns.
- The creator-obligation panel follows the management-detail reference: `履约内容` and `请款内容` are functional top tabs with a black active underline. Each item uses the supplied compact bordered-row format with a light-purple sequence marker, bold key point, content summary, and contract location. The literal field labels `序号`, `重点`, `内容`, and `位置` are hidden, and timing is not repeated in the item. On desktop the panel is capped by the adjacent contract viewer bottom and scrolls internally when needed.
- Contract detail uses `合同信息` and `收款信息` tabs. Contract rows are Advertiser, Publisher, contract amount, fee bearer, and `付款方式/付款渠道`. That final value follows the profile's persisted default payout account as `Bank Transfer/Airwallex`, `Bank Transfer/Payer Max`, or `PayPal/PayPal`; a missing or unknown default renders `待补充` and never falls back to the contract mock's channel. Payout rows are Account Name, Account Number, Beneficiary Bank Name, Beneficiary Bank Address, SWIFT Code, IBAN, and Remittance Information. Creator views show full Account Number and IBAN while administrator reuse remains masked.
- The contract-claim demo presents all six row badges as `合同已明确` and centralizes simulation disclosure in a prominent notice above the claim heading. That notice states the data is for demonstration and not a payment basis, explains that production will parse the contract and IO to synchronize claim fields, and states that blank or unselected clauses will be marked pending confirmation before use.
- When the contract workspace switches to one column at `1180px` or narrower, the creator-obligation panel appears before the contract document viewer. Wide desktop keeps the document on the left and obligations on the right.
- Invoice detail uses the management console's document viewer shell and the supplied formal invoice layout: centered title, billing and creator details, black line-item table, payment information, and signature area.
- All creator-side Invoice detail viewers and downloads use the supplied `INV-20260723-001-Alex-Ruiz.pdf` as their document source. Saved handwritten or generated electronic signatures are overlaid directly below that PDF's `Signature:` label.
- Every Invoice detail page shows a compact prototype notice explaining that the shared Invoice file is only a flow-demo artifact and not a real payment document. The notice states that production will load the project-specific Invoice uploaded from the management console for creator review, signature, and download.
- Invoice detail shows a prominent pre-signing reminder to verify the payment project and payout information. Before signing, the processing card provides an `Invoice 信息有误` feedback path with issue type and details.
- The pre-signing verification reminder and `Invoice 信息有误` action are shown only while an Invoice is awaiting signature. Both disappear immediately after successful signing and remain hidden throughout review, approval, and payment states.
- The Invoice pre-signing reminder states that signing confirms the payment project, payout information, amount, and currency, and that payment failures, returns, and related fees caused by creator-side verification omissions are borne by the creator. In the processing card, the action order is `Invoice 信息有误` → `签署 Invoice` → `下载 Invoice`, with equal spacing between all buttons.
- The `Invoice 信息有误` dialog uses the same typography, spacing, icon container, controls, and action-button sizing as the system's signature dialog.
- Invoice detail provides a toolbar action that opens the document in a full-screen enlarged reader; users can exit with the toolbar action or the Escape key.
- The desktop Invoice list uses six columns in this order: Invoice, related project, channel, amount, status, and actions. Every creator-side Invoice uses the Airwallex channel; mobile keeps a stacked row layout.
- The Invoice list fuzzy search matches only Invoice ID and related project name. A separate payment-channel select filters the list, and both controls combine with the active status tab. Channel options are generated from current Invoice data; the mobile toolbar stacks the controls below the horizontally scrollable tabs.
- Creator Invoice IDs use `INV-YYYYMMDD-00001` with a five-digit sequence that increments within the issue date. Legacy IDs are migrated before display.
- The creator Invoice status navigation order is `全部 → 待确认 → 待审核 → 待修改 → 待付款 → 付款异常 → 已付款`; legacy `DRAFT_SIGNATURE` records are grouped into `待确认`.
- The creator Invoice upload accepts a PDF/PNG/JPG file for a published collection task without preselecting or requiring a payout account. After upload and mock recognition, the creator explicitly selects an eligible account in the payout tab; the creator cannot choose or infer a contract/project relationship.
- Invoice detail follows the selected document-inspection reference with three status/amount/type summary cards, a left document viewer, and two right-side tabs: `Invoice 信息` and `付款状态`.
- Creator-facing internal Invoice type copy is always `Comets内部invoice`. Its payout tab is a read-only `InvoicePayoutSnapshot`; it never allows creator account switching. Existing snapshots are immutable when the profile changes. External Invoice mock recognition of document information and later payout-account selection remain separate.
- The creator home no longer has a full collection list, status filters, a progress column, or pagination. Invoice status details and document actions stay in the Invoice module. Its newest-three update list is sorted by normalized update time.
- Each social-account card uses its official YouTube, Instagram, or TikTok SVG mark, owns its corresponding certification screenshots, and falls back to the generic globe icon for unknown platforms. Legacy screenshot arrays migrate to the first social profile URL. Display name is read-only and synchronizes to the first verified social handle without the leading `@`.
- The creator profile avatar summary keeps the reference's subtle pink-to-blue gradient. Its four independent overview cards share the same fine border, rounded corners, and restrained shadow, but use distinct light blush, lilac-blue, mint, and amber gradients; no accent strips. Long profile forms remain white. Do not expose the internal Creator ID in the profile summary. Social-account guidance asks creators to bind all accounts used for collaboration, while verified accounts hide their screenshot region without deleting saved evidence; unverified/correction states retain evidence and upload access.
- Invoice contact details do not render the read-only Display name field; it remains derived internally from the first verified social handle (without `@`) and stays at its previous value until verification. On the creator profile only, the legal-name label and profile save errors say `真实姓名/公司名 / Real Name/Company Name`; the underlying `legalName` field, registration, and onboarding are unchanged.
- Profile payout navigation is two-level: Bank Transfer / PayPal, then Airwallex / PayerMax under Bank Transfer. Opening the profile selects Airwallex for display but never changes the persisted default account. PayPal and PayerMax remain inspectable and marked unavailable; one account per underlying provider still applies.
- Creator-profile Airwallex Schema and supplemental fields do not render persistent requirement, guidance, or example lines below inputs. Keep required markers, existing in-input format placeholders, field-level blur/save errors, and valid `aria-describedby` references to visible errors. This display decision does not change registration, onboarding, Schema validation, or the mock field metadata.
- Under Bank Transfer, the creator profile shows the current Airwallex / PayerMax provider in the Bank Transfer summary and hides the provider cards until the separate `更换渠道` action expands them; choosing a provider collapses the cards. Returning from PayPal restores the last browsed bank provider without changing the saved default account. Provider switching remains locked while editing an account. The Airwallex transfer-method field normally shows only its selected full-width card, with the method description, scenario-specific mock estimated fee/currency, and estimated business-day range. While editing, `更换方式` expands the available fee-ranked cards for keyboard selection and collapses after selection; the lowest-fee option alone carries the text recommendation, which remains visible among expanded choices. Viewing is read-only. Mock LOCAL estimates 0–2 and SWIFT 1–3 business days; these are not real quotes or promised delivery dates. A failed/empty method load hides stale pricing and prevents saving.
- Creator Invoice detail uses an approximately `60 / 40` desktop split between the document viewer and inspection panel. The right inspection column has a `440px` minimum width; at `1180px` and below, the workspace stacks vertically with the Invoice document before the inspection panel.
- Simulated AI fields include Invoice From, Bill To, Invoice date, Currency, Total, and Payment details. Invoice From is compared with the current profile Real Name, Bill To must equal `COMETS INTERNATIONAL LIMITED`, and the confirmed date/currency/total populate the corresponding Invoice fields.
- Payment-detail matching compares only fields recognized from the uploaded Invoice. Missing fields are filled from the selected payout account. A mismatch blocks confirmation and directs the creator to select another account, edit the profile payout data, or upload a matching Invoice.
- Invoice process history records upload confirmation, review, requested changes, payment readiness, and payment failure or completion. A `CHANGES_REQUIRED` event retains the rejecting account/system and its reason.
- The mock Invoice list includes a current `付款异常` record. Incorrect payout information is a payment-stage failure: the completed review node and blocked payment node remain visible while the creator is routed to the exact bilingual payout field and masked current value. `提交审核` remains disabled until the value changes, Airwallex validation passes, and the updated profile is saved; after submission, review restarts from `资料审核`.
- Payment-failure Invoice actions use distinct semantic colors while retaining equal sizing and spacing: a light logo-aligned coral treatment for `修改付款信息`, green for `提交审核` (including a recognizable muted disabled state), and blue for `下载 Invoice`.
- After a creator corrects payment information and passes the Airwallex Schema validation, the payment-failure action is `提交审核`, not `重新发起付款`. The only valid transition is `PAYMENT_FAILED → PENDING_REVIEW`; review restarts from the creator-facing `资料审核` node (the media-review entry point), and the payment node returns to not started until review passes again.
- The unsigned-Invoice flow always includes a stable pending-signature record. Creators can view or download it, then either draw a handwritten signature or select from several electronic signatures generated from the Airwallex payout account holder name. Both methods require explicit consent to a legal-effect declaration before confirmation; confirmation persists the signature below `Signature:` and advances the Invoice to pending review. Legacy uploaded-signature records remain readable, but upload is no longer offered in the signing UI.
- Submitting `Invoice 信息有误` closes the feedback form and opens a centered success dialog. Issue-feedback success never uses the persistent page-level notice; signature and Invoice status-transition notices keep their existing page-level behavior.
- Treat `http://localhost:4173` as the test environment and `http://192.168.88.188:8772` as the user environment. Apply requested changes locally by default and update the remote Mac only after the user explicitly says to push.
- Responsive support targets `320×568`, `360×800`, `375×812`, `390×844`, `430×932`, `768×1024`, `1024×768`, `1366×768`, `1440×900`, `1920×1080`, `2048px`, and `2560×1440`. Desktop navigation remains fixed above `1100px`; iPad widths use the drawer and touch-friendly list layouts. Creator and administrator business pages share fluid `max-width` + `margin-inline: auto` containers centered within the area to the right of their respective sidebars. At viewport widths of `2048px` and `2560px`, both sides of that area have `87.4px` and `246px` gutters respectively; above `2560px` the content cap stays fixed. Full-width topbars and the existing registration/onboarding composition are excluded. Page-level horizontal scrolling is prohibited; tabs, document readers, and irreducible tables may scroll only inside their own containers.
- Registration progress nodes are semantic keyboard-accessible controls. Completed steps can be revisited without losing the current onboarding draft; returning to step one after account creation shows the registered email in read-only mode and never repeats registration or exposes the password. Onboarding drafts are isolated by creator, cleared after completion or logout, and never persist passwords.
- Airwallex transfer methods are loaded through the payout service for the selected country, currency, and recipient type, sorted by estimated fee, and labeled with the lowest-fee recommendation. The lowest-fee available method is selected initially and whenever those three scenario fields change; a manual selection remains stable until the scenario changes or the method becomes unavailable. Form Schema loading starts only after the transfer method has been resolved.
- Registration step 3 reuses the profile payout-scenario transfer-method card: show the selected method, lowest-fee and selected badges, estimated fee and arrival time; "更换方式" reveals the available methods. Its fee/arrival disclaimer remains visible, and changing a method reloads the dynamic Airwallex Form Schema. Preserve the existing profile presentation and small-screen layout.
- Request status mapping is `待签署 → 审核中 → 待付款 / 付款异常 → 已完成`, derived from `DRAFT_SIGNATURE`, `PENDING_REVIEW`, `APPROVED` / `PAYMENT_FAILED`, and `PAID`. Multi-currency request summaries display each currency total separately.
- The three request-homepage amount-summary information icons are functional tooltips. They explain the status scope behind each total and must remain accessible by hover, keyboard focus, and mobile tap.
- The topbar help action navigates to `/help`, retaining the creator-facing workflow guide, shortcuts, and pre-signing reminder.
- The creator sidebar has a separate `/notifications` navigation link with the same unread count as the topbar bell; the bell also continues to navigate to `/notifications`. Mobile sidebar navigation closes the drawer. Administrator sidebar navigation is unchanged. The creator notification center retains individual and bulk read actions, local persistence, and deep links to related details.
- The administrator root route is the all-creators request-project overview. Its sidebar contains request projects, contracts, Invoice, and an expandable system-settings group with user management and operation logs.
- Administrator request-project, contract, and Invoice modules aggregate every creator's available business records and remain strictly read-only.
- Administrator contract seed data is owned only by creators already present in user management. The current distribution is Léa Martin 8, Camille Dubois 3, Noah Williams 3, Maya Sato 2, and Alex Ruiz 2 contracts. Across the ten contracts added for the latter four creators, seven have matching Invoices and three remain contract-only; contract-only records must not appear in request-project or Invoice modules, but their lifecycle is still synchronized independently.
- The administrator request-project overview uses four summary cards arranged as a two-column, two-row grid on desktop and tablet because a single four-column row is too dense. The fourth `项目总数` card is calculated from the unfiltered system-wide request-project set and shows the overall project count plus a separate total amount and project count for every currency. Mobile stacks the four summaries in one column.
- The administrator contract summary is `全部合同 / 待签署 / 执行中 / 已过期` with system totals `18 / 5 / 7 / 6`; the four metrics use the same white segmented summary surface and collapse responsively to two columns and then one.
- The administrator Invoice page starts with a system-wide six-metric summary in this order: `Invoice 总数`, `待签署`, `待审核`, `已通过审核`, `付款异常`, and `已打款`. Counts always use the unfiltered all-creator Invoice set and use the same white segmented surface as other administrator summaries. To avoid crowding, desktop renders the six metrics as two rows of three; tablet uses two columns and mobile uses one.
- Administrator business seed data uses the creator accounts from user management. The system-wide Invoice set currently contains 13 records distributed across Léa Martin, Camille Dubois, Noah Williams, Maya Sato, and Alex Ruiz as `6 / 2 / 2 / 2 / 1`. Every Invoice must match a contract owned by the same immutable Creator ID and reuse its project ID, normalized project name, and brand; split Invoice amounts need not equal the contract total. Contract-only records do not produce request projects, and administrator business records remain read-only.
- Every complete administrator list uses the shared pagination control with a default of 20 rows and page-size options `20 / 50 / 100`. Its durable visual reference is the supplied compact single-line control: `共{n}条`, borderless previous/page-number/next controls, a purple circular outline for the current page, and a rounded `{size}条/页` selector on the right. Large page sets use clickable ellipses for direct valid-page entry instead of a permanently visible jump field. Search or filter changes reset the list to page 1. User-management bulk selection applies to the current page while preserving selections from other pages, and exports still include the complete filtered result. Compact dashboard preview lists remain capped previews and link to their paginated modules.
- Every administrator `查看` action for a request project, contract, or Invoice opens an administrator-scoped read-only detail route that reuses the corresponding creator detail content and document viewer. These three detail pages do not show a separate creator-context or administrator-read-only reminder banner; they preserve links among the matched request, contract, and Invoice and never expose creator-only signing, payout-editing, or payment-retry actions.
- Administrator search, select, and date-filter controls reuse the creator-side Invoice toolbar interaction language: 38 px white fields, 8 px corners, neutral borders, Lucide leading icons, light-purple keyboard focus rings, and restrained active-filter feedback. Search fields expose a clear action after input; business filters group search and creator selection separately from status tabs; mobile stacks controls without horizontal overflow.
- External Invoice collection is task-driven and Invoice-number-centric. Creators cannot create an external Invoice from a project selector; every creator-facing task, list row, detail heading, notification, timeline, download action, and error uses the immutable internal `invoiceId` for commands and `INV-YYYYMMDD-00001` as the only visible collection identifier.
- External Invoice document status (`DRAFT / WAITING_UPLOAD / RECOGNIZING / WAITING_CONFIRMATION / WAITING_MEDIA_REVIEW / RETURNED_FOR_CORRECTION / RETURNED_FOR_REUPLOAD / APPROVED / RECOGNITION_FAILED / CANCELLED`) remains independent from payment status (`WAITING_PAYMENT / PROCESSING / PAID / FAILED`). A payment failure never reopens or downgrades an approved document.
- External Invoice mutations are typed, version-checked, creator-owned, idempotent service commands. Components never write workflow state or localStorage directly. Re-upload appends immutable file versions; simulated AI recognition and creator corrections append recognition and confirmation snapshots instead of overwriting originals.
- Successful simulated AI recognition is rendered immediately in the current Invoice page and never creates a notification. MockApiAdapter never claims to parse arbitrary uploaded PDFs/images or fabricates recognized fields from expected task/account values; unrecognized fields require creator review. Notifications are limited to action-required or business-result events and never contain project/brand names, bank-account values, or payment execution identifiers.
- External Invoice upload bytes live in browser IndexedDB, not the localStorage Invoice JSON. The latter keeps stable file keys and version metadata; preview/download resolve the stored bytes, and legacy embedded Data URLs migrate without discarding the original on failure. This remains prototype-only local storage.
- External Invoice information confirmation requires a nonblank ticket `Description` for new submissions. Display it between Invoice date and Currency; the mock recognition leaves it empty rather than inventing text from the project, contract, or Invoice type. Creator corrections preserve the immutable original recognition and are scoped to the current file version; re-upload keeps historical snapshots but requires a fresh Description. Previously submitted or approved records lacking the optional stored field remain readable without a retroactive status change. Administrator views remain read-only.
- Uploaded PDF documents use a local single-page-at-a-time vertical reader rather than the browser's native PDF iframe; images remain single-page. External Invoice information and selected payout account each require persisted file-version-bound confirmation before final audit submission. Invoice-information rows compare demo values with system requirements, while payout rows show only the selected profile account's full fields. The deterministic mock values are explicitly not parsed from the uploaded file.
- The current creator may see complete payout-account fields only in their own external Invoice detail and account-comparison views. Creator tasks and notifications omit banking values; administrator reuse continues to mask sensitive values and exposes no creator action controls.
- Creator-facing external Invoice surfaces must not render project or brand names, Project/Engagement IDs, payment-order/detail/batch IDs, payout/transfer IDs, or channel transaction IDs. Those values may remain internal to the adapter for integration compatibility.
- Mobile contract-list cards navigate to contract detail from any non-action region; their view and download controls remain independent. Contract detail headings do not show a file/update subtitle.
- Invoice detail shows a compact linked-contract card below the three metrics only when the creator-owned Invoice-to-contract relationship is confirmed. The card displays contract number and lifecycle status, never a project name, and links to the creator or administrator-scoped contract detail as appropriate.
- Invoice detail separates `Invoice 信息`, `收款信息`, and `付款状态` into three right-panel tabs. The payment tab shows amount, status, dates, failure reason, lifecycle, and history without duplicating payout-account fields. Internal Invoices show the creator's current default account before signing and freeze its full details in the Invoice payout snapshot upon signing; external Invoices display only the chosen account, without ticket-to-account comparison. Pending-signature controls sit in the right-panel footer as `Invoice 信息有误` then `确认并签署`; the footer download action is removed while the page and viewer downloads remain. Completed/current/failed/pending timeline nodes use green/blue/red/neutral treatments with text labels. Before document approval, payment display and payment nodes remain `未开始` even if the stored payment state is `WAITING_PAYMENT`.
- An internal Invoice's `Invoice From` is an Invoice-owned legal-name value synchronized from the management system and fixed in the mock record; profile edits do not silently rewrite it. External Invoice simulated AI recognition retains its original `Invoice From` alongside creator corrections and Real Name validation. Neither value is derived from the contract Advertiser brand.
- External Invoice ticket currency is compared only with the system's expected Invoice currency, never with the chosen payout-account currency. Ticket payment details are retained as historical data but never compared with the selected account or used to block its confirmation. Before account selection, show an explicit empty state. The three right-panel tabs keep one card height at each viewport; content scrolls inside the card while tabs and signing footer stay available.
- The new administrator experience is an incremental, read-only mode over the existing creator pages. Administrator login lands on `/admin/select-user`; a verified Creator ID scopes `/home`, contracts, Invoices, payments, and profile with `?userId=...`. Keep the original administrator source, layout, and routes directly accessible for comparison and rollback; user management and audit logs remain fully operational. An invalid or missing Creator ID never falls back to another person. Preserve creator interactions unchanged, mask administrator payout/history data, and block original document access and creator writes at the mock service boundary. Do not remove old routes or deploy to `192.168.88.188:8772` without explicit approval.
- Administrator daily navigation excludes the all-user request-project and business-list modules. It shows only the selected creator's business pages plus user management and audit logs; legacy administrator business routes remain directly accessible for comparison, but are hidden from the default navigation. The administrator Logo returns to creator selection.
- Pending initial verification means no active contracts, Invoices, or collection projects. Existing mock records for pending creators are archived in versioned local state and excluded from lists, details, and totals; later verification does not resurrect them without a fresh external sync event. Creators in `CHANGES_REQUESTED` retain their prior business history. Reject and log new external business events while initial verification is pending.
- The selected creator's administrator summary has four independent white cards. The legacy administrator contract and Invoice summaries have four and six independent white cards respectively. Creator selection and payment-record lists use the creator Invoice list's typography, row density, responsive anatomy, and empty-state style. The creator-selection shortcuts are `全部达人 / 待认证 / 待修正`, without invitation filtering.
- Administrator user management and audit logs keep all identity-operation features but use the shared creator workspace visual shell and page/list controls. Their routes work without a selected creator and preserve a valid return-to-creator context. Make changes only on the local test server until explicitly asked to push to the Mac.
